
export const Header =()=>{
    return(
        <header className={'header'}>
            <nav className={'navigation'}>
                <ul className={'navigation-link'}>
                    
                </ul>
            </nav>
        </header>
    )
}